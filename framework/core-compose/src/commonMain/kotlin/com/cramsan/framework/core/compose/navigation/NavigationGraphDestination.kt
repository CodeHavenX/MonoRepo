package com.cramsan.framework.core.compose.navigation

/**
 * Destination representing the entry point of a navigation graph.
 *
 * Consider a set of logically related screens. For example the sign in and sign up [Destination]s could be grouped
 * into a single [NavigationGraphDestination] called "Auth".
 */
interface NavigationGraphDestination
