package com.cramsan.templatereplaceme.server.settings

import com.cramsan.architecture.server.settings.SettingKey

/**
 * Application setting keys for the TemplateReplaceMe server.
 *
 * Add new configuration keys here as [SettingKey] properties.
 * Keys are read from the application configuration file at startup.
 */
object TemplateReplaceMeSettingKey {
    /** Example boolean setting. Replace with your application's real configuration keys. */
    val ExampleEnabled = SettingKey.boolean("example.enabled")
}
