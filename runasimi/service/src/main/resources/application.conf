ktor {
    deployment {
        port = 8282
        port = ${?PORT}
    }

    application {
        modules = [ com.codehavenx.platform.bot.ApplicationKt.module ]
    }
}

kord {
    container_port = ${?CONTAINER_PORT}
}