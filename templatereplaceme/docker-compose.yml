version: '3.8'
services:
  backend:
    image: 'ghcr.io/codehavenx/template-replace-me-backend:latest'
    restart: unless-stopped
    pull_policy: always
    environment:
      - TEMPLATE_REPLACE_ME_ALLOWED_HOST=${TEMPLATE_REPLACE_ME_ALLOWED_HOST}
  frontend:
    image: 'ghcr.io/codehavenx/template-replace-me-frontend:latest'
    restart: unless-stopped
    pull_policy: always
