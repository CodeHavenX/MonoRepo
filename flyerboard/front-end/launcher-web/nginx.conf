server {
    listen 80;

    root /usr/share/nginx/html;

    location / {
        # SPA fallback so a direct load or refresh of a deep-linked path (e.g.
        # /archive) serves index.html instead of 404ing.
        try_files $uri $uri/ /index.html;
    }
}
