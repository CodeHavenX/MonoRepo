FROM eclipse-temurin:21-jre

ARG JAR_URL
RUN test -n "JAR_URL"
ENV JAR_URL=$JAR_URL

WORKDIR /app

# Download the JAR
RUN curl -L $JAR_URL -o service.jar

# Run the JAR
ENTRYPOINT ["java", "-jar", "service.jar"]